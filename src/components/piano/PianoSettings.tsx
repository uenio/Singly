import React, { useState } from 'react';
import { listNotes } from '../../helpers/Notes';
import { Key } from './Key';
import { NoteInformation } from './Note';

export default function PianoSettings() {
    // BPM, Show Keys, Min, Max, Limit?, Playable?
}
